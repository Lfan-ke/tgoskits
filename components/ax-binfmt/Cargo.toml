[package]
name = "ax-binfmt"
version = "0.1.0"
repository.workspace = true
edition.workspace = true
authors.workspace = true
description = "Executable formats: recognize an image by its magic and hand it to the loader that claims it (Linux ELF / Windows PE / macOS Mach-O)."
keywords = ["binfmt", "personality", "abi", "no-std"]
categories = ["no-std", "os"]
license.workspace = true

[dependencies]
ax-dispatch = { workspace = true }
bitflags = { workspace = true }
thiserror = { workspace = true }
