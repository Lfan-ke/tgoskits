[package]
name = "ax-abi-path"
version = "0.1.0"
repository.workspace = true
edition.workspace = true
authors.workspace = true
description = "Path and symbol compatibility for foreign personalities: translate Windows/NT paths to the POSIX form ax-fs expects."
keywords = ["path", "windows", "compat", "no-std"]
categories = ["no-std", "os"]
license.workspace = true

[dependencies]
