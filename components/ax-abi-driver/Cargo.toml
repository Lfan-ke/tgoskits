[package]
name = "ax-abi-driver"
version = "0.1.0"
repository.workspace = true
edition.workspace = true
authors.workspace = true
description = "Driver ABI compatibility for foreign personalities: decode NT device control codes and route device I/O onto ax-driver."
keywords = ["driver", "ioctl", "windows", "no-std"]
categories = ["no-std", "os"]
license.workspace = true

[dependencies]
